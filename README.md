# React + TypeScript + Vite
This little project was made using Typescript and Redux.It contains all todo items and all the todo actions
                (editing,deleting,adding,filtering) in redux state. All the todos are also stored in localStorage so that you 
                can retrieve toDo data even after closing or refreshing a page.
